const escCommunity=value=>String(value??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));const face=nick=>`https://mc-heads.net/avatar/${encodeURIComponent(nick||"Steve")}/96`;const roleSlug=role=>String(role||"membro").normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/\+/g,"-plus").toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"")||"membro";const renderPeople=(items,target,type)=>{const seen=new Set();const uniqueItems=items.filter(person=>{const key=person.id||`${person.nick}|${person.role}|${person.discord}`;if(seen.has(key))return false;seen.add(key);return true});target.innerHTML=uniqueItems.map(person=>`<article class="person-card ${type==="creator"?"creator-card":""} role-${roleSlug(person.role)}"><div class="person-card-top"><span class="role-icon" aria-hidden="true">◆</span><span class="person-role">${escCommunity(person.role)}</span></div><img class="person-face" src="${face(person.skinNick||person.nick)}" alt="Skin de ${escCommunity(person.nick)}"><h2>${escCommunity(person.nick)}</h2><p>${escCommunity(person.discord)}</p>${person.description?`<p>${escCommunity(person.description)}</p>`:""}</article>`).join("")||"<div class=\"catalog-empty\">Nenhum integrante cadastrado ainda.</div>"};document.querySelector("#community-year").textContent=new Date().getFullYear();document.querySelectorAll("[data-community-tab]").forEach(button=>button.addEventListener("click",()=>{document.querySelectorAll("[data-community-tab],.community-view").forEach(element=>element.classList.remove("active"));button.classList.add("active");document.querySelector(`#community-${button.dataset.communityTab}`).classList.add("active")}));Promise.all([fetch("/api/team").then(response=>response.json()),fetch("/api/creators").then(response=>response.json())]).then(([team,creators])=>{renderPeople(team,document.querySelector("#team-grid"),"team");renderPeople(creators,document.querySelector("#creators-grid"),"creator")});