const rulesBox=document.querySelector("#rules-content");document.querySelector("#rules-year").textContent=new Date().getFullYear();fetch("/api/settings").then(response=>response.json()).then(settings=>{rulesBox.innerHTML=String(settings.rulesText||"").split(/\n\s*\n/).map(paragraph=>`<p>${paragraph.split("\n").map(line=>line.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))).join("<br>")}</p>`).join("")});